# DomainViewer Frontend Environment Variables
# Copy this file to .env.local and update the values

# Backend API is proxied via Next.js rewrites (see next.config.ts)
# No NEXT_PUBLIC_API_URL needed — frontend calls /api/* which is rewritten to backend
